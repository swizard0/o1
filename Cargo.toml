[package]
name = "o1"
version = "0.1.0"
authors = ["Alexey Voznyuk <me@swizard.info>"]

[features]
default = ["with-rayon"]
with-rayon = ["rayon"]

[dependencies]
rayon = { version = "1.0", optional = true }

[dev-dependencies]
rand = "0.5"
